# generated by rules_pydeps

load("@rules_pydeps//pydeps:pydeps.bzl", "pip_deps_index")
load("{{pip_requirements}}", "all_requirements", "requirement")
load("//:pins.bzl", "pins")

pip_deps_index(
    name = "pip_deps_index",
    pins = pins,
    requirement = requirement,
    all_requirements = all_requirements,
)
